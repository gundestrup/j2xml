-- Add token column to #__j2xml_websites for REST API authentication.
-- This table is created by J2XML Pro; the column is added only if the table exists.
-- The check is done in script.php (postflight) to avoid SQL errors on fresh installs.
SELECT 1;
