{
  "name": "j2xml-pako-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build script to produce a browser-compatible pako inflate bundle for J2XML",
  "scripts": {
    "build": "esbuild entry.js --bundle --format=iife --global-name=pako --outfile=../../media/lib_eshiol_j2xml/js/pako_inflate.js --banner:js='/* pako 3.x inflate-only bundle (built from npm pako 3.0.1) */'"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "pako": "^3.0.1"
  }
}
